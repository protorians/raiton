export enum ParamTypeEnum {
    APP = "application",
    PARAM = "param",
    BODY = "body",
    QUERY = "query",
    HEADER = "header",
    REQ = "req",
    RES = "res",
    UPLOAD_FILE = "upload",
    CUSTOM = "custom",
}