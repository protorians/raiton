export enum Parametrable {
    PARAM = "param",
    BODY = "body",
    QUERY = "query",
    HEADER = "header",
    REQ = "req",
    REPLY = "reply",
    UPLOAD_FILE = "upload",
    CUSTOM = "custom",
}